# PNG Restart Marker - DRAFT

This extension introduces restart markers for PNG images
with a new chunk and a backward-compatible encoding method,
it allows for non-linear decoding and encoding of image data.

